{
  "routes": [
    {
      "method": "GET",
      "path": "/needs",
      "handler": "need.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/needs/count",
      "handler": "need.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/needs/:id",
      "handler": "need.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/needs",
      "handler": "need.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/needs/:id",
      "handler": "need.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/needs/:id",
      "handler": "need.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
